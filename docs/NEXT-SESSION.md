# Next: STOP 3, hand tuning

The STOP 2 refinement round and packet I12 landed 2026-07-07 (commits
`91b1460` and the I12 commit that follows it); the execution records live
in the plan and the orchestration ledger, and STATE-OF-PLAY is current.
Both parts are installed to /Applications/Conn.app.

What remains in Block A is Samay-driven, not a coding session:

- STOP 3: drive the tuning playground (`cd macos && ./.build/debug/Conn
  --preview`, or the installed app's preview) and the live hotkey until
  summon, breath, exhale, chip, and belay feel award-grade. Write Back
  puts tuned values in DesignTokens.swift and prints the spec-table diff;
  tokens and spec tables land in one commit.
- The deferred 12-principles adversarial pass and the Fable taste pass on
  a recording fold into STOP 3 (from I10's done-definition).
- Residue from STOP 1: the wifi-kill and PTT-during-thinking drill steps,
  folded into the next live session.

Do not start Block B (STOP-G, R5, X1) without Samay's word.

Delete this file when Block A closes at STOP 3.
