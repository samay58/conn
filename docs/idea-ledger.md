# Conn idea ledger

Living log of rejected and deferred ideas so nothing gets silently dropped. An idea leaves this file only by being built or by being re-rejected with a new entry. Revisit triggers are concrete: a trace pattern, a felt complaint, or a named milestone, never "someday".

| Idea | Status | Why not now | Revisit trigger |
|---|---|---|---|
| Cute character in the notch | Deferred 2026-07-05 | Taste risk this round; motion-personality skeleton ships first with an aliveness scalar as the mount point | Island motion personality feels solid for 2+ weeks of daily use and still wants more charm |
| Voice approval ("approve it" spoken) | Rejected 2026-07-05 | Speech recognition on the approval path; a mishear approves a real action | A future verified-phrase mechanism with zero-false-positive evidence, plus an act_confirm tool used 10+ times/day where clicking measurably hurts |
| Keyboard approval | Rejected 2026-07-05 | The stray-Return incident class; panel must never take key focus | Only alongside a design that provably cannot route typed input to the panel (e.g. dedicated hardware key), and user demand in felt notes |
| Persistent idle island | Rejected 2026-07-05 | Conn is push-to-talk, not ambient presence; battery and attention cost | User reaches for session state (cost, connection) often enough that summoning to check becomes a logged annoyance |
| Hover-near-notch reveal | Deferred 2026-07-05 | Discoverability affordance without clear need yet | Same trigger as persistent island |
| Console visual parity | Rejected 2026-07-05 | Console is the frozen debug surface; craft budget goes to the island | Console becomes a daily surface for someone other than debugging |
| Native typed input | Deferred 2026-07-12 | Voice remains the product bet; the console no longer accepts commands | Repeated user demand for a non-voice command path after semantic acceptance |
| Full onboarding flow | Deferred 2026-07-05 | Single-user product today; reliability lane fixes the silent failures first | Second real user, or Mac Mini deployment becomes routine |
| Screenshot-to-model image input and visual action lane | Deferred 2026-07-12 | The semantic engine is implemented without ScreenCaptureKit, OCR, image injection, or coordinate actions. Its real fixture, live matrix, and product gates remain open | Semantic live and product gates pass, then a separate visual fixture proves zero wrong-window actions, at least 95 percent static target accuracy, and confirm-gated coordinate dispatch |
| Semantic UI control (press, type, scroll, tabs, menus, key chords) | Implemented 2026-07-12; acceptance open | Native observation, target re-resolution, bounded plans, evidence predicates, dispatch certainty, and receipts cover the semantic slice. Ambiguous and secure targets refuse. Production has no Python AX/input fallback | Real fixture matrix, six-app live matrix, and product gate pass |
| MCP adapters | Deferred 2026-07-02 (shipped spec) | Tool surface still small enough to own locally | A wanted capability that already exists as a good MCP server |
| Phoenix write lane (voice capture into vault) | Deferred again 2026-07-12 | Pointer approval exists, but semantic and product acceptance still need attention | Semantic and product gates pass, then traces show recurring capture demand |
| Shell command execution | Deferred 2026-07-02 (shipped spec) | Highest blast radius, zero gates designed for it | Per-app profile + allowlist design exists and a real need shows in traces |
| `conversation.item.truncate` barge-in precision | Deferred 2026-07-02 (shipped spec) | Cancel-and-flush is acceptable in v0 | Felt complaint that barge-in clips or overlaps audibly |
| General-tool capability breadth (north star) | Partially implemented 2026-07-12 | The semantic engine supplies flexible native operations without app command catalogs. Visual-only controls, destructive/outbound work, and broad profiles remain absent | Semantic live and product gates pass; traces then identify one concrete unsupported task class |
| External-display top-center island and broader surface reframe | Deferred again 2026-07-12 | Mac mini displays use the floating panel because no physical notch exists. User expects a more island-like top-center surface, but restart reliability and semantic proof remain higher leverage | Semantic acceptance passes, or the panel itself repeatedly blocks testing or daily use |
| Extract conn into a standalone repo, out of the phoenix vault | **Built 2026-07-07** | Executed: `~/conn` is its own repo with its own `.venv` and a fresh, self-contained history (earlier development history stays in the private monorepo). Symlink kept at the old path for internal references. DaemonLauncher defaults, docs, and deployment guide rewired; full suite, evals, doctor, Swift tests, and app rebuild verified after the move. Published to `github.com/samay58/conn` 2026-07-07, replacing the retired subtree mirror | Done |
| Silence the PaMacCore err -50 on audio teardown | Deferred 2026-07-08 | One-line PortAudio/AUHAL noise on device close during daemon shutdown (bug 5 residue; the Broadcaster writer half of that bug is fixed). Cosmetic: no user-visible effect, no trace impact. Chasing it means reworking audio stream close ordering in audio.py against a third-party teardown path | The -50 line appears outside shutdown, or any audible artifact at session end lands in a felt note |
| Host the real `IslandView` inside `Conn --preview` instead of the parallel `IslandPreviewSurface` reimplementation | **Built 2026-07-07** | Packet I9 retargeted PreviewWindow at the canonical IslandView and added the state cycler plus screenshot rig | Done |
| Grounded semantic lane through Conn.app | Implemented 2026-07-12; acceptance open | Conn.app owns production snapshots, target identity, re-resolution, dispatch, and evidence verification. Python remains policy and orchestration only. Production cannot enable the legacy mutation engine | Real fixture, live matrix, and product gates pass |
| Broad app-specific command profiles | Deferred 2026-07-10 | The semantic engine is meant to generalize from current native state. Catalogs would hide ambiguous targeting and become version-brittle | A named app repeatedly fails live probes for a stable, documented semantic reason that cannot be fixed generically; any profile must carry its own fixture and version boundary |
| Multi-action macro tool | Rejected 2026-07-10 | Fresh observation and one mutation per response are core safety properties. A macro tool would recreate stale targets and hidden partial completion | Only after transaction composition has a formal partial-failure model and traces prove repeated replanning is the dominant usability cost |
